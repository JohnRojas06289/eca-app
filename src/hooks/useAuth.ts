// Re-exporta desde el contexto global para mantener compatibilidad con imports existentes
export { useAuth, AuthProvider } from '@/src/context/AuthContext';
export type { AuthUser, UserRole } from '@/src/context/AuthContext';
